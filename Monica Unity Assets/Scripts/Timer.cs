/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;*/

// Monica Ly 

// public class Timer : MonoBehaviour
// {
    /* Keeps track of time
     * Keeps track of time between scene loading for transition duration to match duration of 02_Stage Start.mp3 (4s) 
     * 
     */

    // https://docs.unity3d.com/ScriptReference/Time.html
    // https://docs.unity3d.com/ScriptReference/Time-deltaTime.html
    // timer adds deltaTime each frame

    // ==== VARIABLES ====
    // public static float deltaTime;
 //   public float transitTimer = 0.0f; // count s for transition
    // https://docs.unity3d.com/ScriptReference/Time-timeScale.html
    // When timeScale is set to zero the game is basically paused if all your functions are frame rate independent.
    
    // public static float Time.timeScale; // When timeScale is 1.0 time passes as fast as realtime
    // Time.timeScale = 1.0f;

 //   public float transitDuration = 4.0f; // 4s duration

    // Start is called before the first frame update
 //  void Start()
//    {

//    }

    // Update is called once per frame
//    void Update()
//   {
//        transitTimer += Time.deltaTime; // https://docs.unity3d.com/ScriptReference/Time-deltaTime.html

//       if (transitTimer > transitDuration) {
            // sceneCounter += 1;
            // TransitionScene();
//        }



//    }
// }
